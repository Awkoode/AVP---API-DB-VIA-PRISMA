# api foda

## Como executar o projeto

1. Instalar as dependências:
   ```bash
   npm install
   ```
   
2. Migração do db prisma

``` bash
npx prisma migrate dev
```
3. Rodar Servidor

``` bash
   node server.js
   ```